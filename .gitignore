# Python
__pycache__/
*.pyc
.venv/

# Server lives in its own nested git repo — not tracked by the plugin repo.
server/

# Local test artifacts (state always lives in user's project dir, not here)
.ditat_token_*.json
.ditat_batch.json
.ditat_findings.json
downloads/
reports/
.env

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code per-user local settings (allowlist, env)
.claude/settings.local.json
